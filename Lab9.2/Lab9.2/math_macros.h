#pragma once
#include <cmath> // Include cmath for the pow function

#define MAX(a, b) ((a) > (b) ? (a) : (b))
#define MIN(a, b) ((a) < (b) ? (a) : (b))
#define ABS(a) ((a) < 0 ? -(a) : (a))
#define POW(a, b) (pow((a), (b))) // Use pow from cmath for powers
#pragma once
